import { OpenPoliceDirective } from './open-police.directive';

describe('OpenPoliceDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenPoliceDirective();
    expect(directive).toBeTruthy();
  });
});
